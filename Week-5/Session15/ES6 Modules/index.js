// Importing the add and subtract functions from calculator.js
import { add, subtract } from "./calculator.js";

// Using the add function to add two numbers and log the result
const sum = add(5, 3);
console.log('Sum:', sum);

// Using the subtract function to subtract two numbers and log the result
const difference = subtract(10, 7);
console.log('Difference:', difference);